# [avif.now](https://avif.now)

## build

Installing the dependencies requires `make`, `cmake`, Emscripten, and `pkg-config` available in the environment.

```
pnpm i
```

Build the static site to `dist/`:

```
pnpm run build
```
